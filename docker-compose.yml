
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "PostgresEasyTag!"
      POSTGRES_USER: "postgres"
      PGPORT: 5433
    ports:
      - "5433:5433"
    volumes:
      - ${HOME}/postgres:/var/lib/postgresql/data